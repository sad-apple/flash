namespace: 36b5a6d6-37ac-4cfd-aa3a-4d308d954539
group: cloud-demo

spring:
  application:
    name: cloud-consumer
  cloud:
    nacos:
      server-addr: 127.0.0.1:8848
      password: nacos
      username: nacos
      discovery:
        namespace: ${namespace}
        group: ${group}
      config:
#        namespace: c7f7f7b6-00cc-45c0-bab1-6dd98653a589
        namespace: ${namespace}
        group: ${group}
        file-extension: yaml
        refresh-enabled: true
  config:
    import:
      - optional:nacos:cloud-consumer-q.yaml?group=${group}
      - optional:nacos:logback-nacos.xml

#server:
  #  port: 8071
#server:
#  servlet:
#    context-path: cloud-demo